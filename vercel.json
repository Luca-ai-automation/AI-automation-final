{
  "version": 2,
  "builds":{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}

  ],
  "routes": [
    { "src": "^/api/chat$", "dest": "/server.js" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
